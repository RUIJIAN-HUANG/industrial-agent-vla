# CODEOWNERS 暂不启用真实规则：团队尚未确认 6 名成员的 GitHub 用户名。
# A 收齐用户名后，应在一个独立 PR 中用真实 @username 取消注释并替换示例。
# 不要保留虚构用户名；无效账号会导致自动 Review 和分支保护失效。
#
# 参考模板（仅注释，不会生效）：
#
# *                                      @<member-a> @<member-f>
# /src/industrial_agent/                 @<member-a>
# /simulation/                           @<member-b>
# /data/                                 @<member-c>
# /services/openvla_oft/                 @<member-d>
# /services/pi05/                        @<member-e>
# /tests/                                @<member-f>
# /.github/                              @<member-a> @<member-f>
# /docs/official/                        @<member-a>
# /docs/project-management/              @<member-a> @<member-f>
#
# 注意：
# 1. GitHub 使用“最后一条匹配规则”，规则顺序会影响结果。
# 2. 用户或团队必须对仓库具有所需权限。
# 3. 启用规则后，用测试 PR 验证自动请求评审，再开启 Code Owner 强制审批。
