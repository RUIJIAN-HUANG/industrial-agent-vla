# ADR-0003：YOLO 作为三 Agent 架构中的感知旁路

状态：已实现

YOLO 是唯一感知 Agent。它只消费 CAS 图像并生成检测证据，不调用 π0.5、不规划
动作、不改变控制令牌。总控可以同步调用 YOLO，但 YOLO 的空检测、超时或暂时不可用
不会阻塞 π0.5 的主控制路径。

总控和 π0.5 共享同一份图像引用；YOLO 的检测结果只用于在线证据和离线评测，不能
把 GT 或特权坐标带入模型请求。
