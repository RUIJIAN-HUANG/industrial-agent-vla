# XH-202607 项目看板

> 更新时间：2026-08-29
> 状态只按仓库可复现证据更新。

## 当前架构

| Agent | 责任 | 当前状态 |
|---|---|---|
| 总控 Supervisor | 任务、FSM、令牌、安全、审计 | 已有 V2 运行时与 mock 契约 |
| YOLO | 同步检测证据与独立评分 | 已有独立服务与合同测试 |
| π0.5 | 唯一 VLA，按 `arm_id` 串行服务 Arm_A/Arm_B | 已有适配器、服务入口与路由测试 |

两台机械臂是执行机构，不是额外 Agent。生产 Compose 只启动 `pi05` 和 `yolo`；
总控运行在主进程或 Isaac 入口中。P01/W01 只激活 Arm_A，料箱搬运任务按
Arm_A→Arm_B 串行交接，这个行为由任务合同决定。

## 证据状态

| 维度 | 状态 | 说明 |
|---|---|---|
| 架构与接口 | 已实现 | 三 Agent 边界、单一 π0.5、YOLO sidecar 和 CAS 合同已冻结 |
| 自动化测试 | 需 CI 复核 | 使用 `python -m pytest -q`，数量以最近一次成功 Run 为准 |
| Isaac Sim 闭环 | 未验收 | 仍需真实平台 headless、IK、抓取和连续回合证据 |
| π0.5 双臂训练 | 未验收 | 协议已支持两臂，模型数据和真实动作效果仍需分别验证 |

## 门禁

1. 先通过场景、观测、CAS 和安全合同预检；
2. 再分别验证 π0.5 的 Arm_A/Arm_B 路由；
3. 交接区仍由 Supervisor 以令牌和 3 帧 2 票规则管理；
4. 任意安全、观测或预算错误都必须 safe-stop。
