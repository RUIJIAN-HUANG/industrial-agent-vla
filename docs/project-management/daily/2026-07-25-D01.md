# D01 每日任务公告｜2026-07-25

> 当日 Gate：基线与协作准入
> 09:00 开始，17:00 交付，18:00 集成
> 唯一目标：冻结需求、版本、任务族和协作入口，让六名成员各有一个可验收的
> Draft PR，并为 D02 平台 Gate 准备完整证据。

## 1. A-F 任务

| 角色 | P | 今日唯一主任务 | 必交文件/证据 | Definition of Done | 依赖 |
|---|---|---|---|---|---|
| A | P0 | 主持 45 分钟基线会；把 `REQ-*`、`SCORE-*`、三任务族和 D01-D12 拆成 Issue | 基线会议决定、Issue/里程碑链接、TaskPlan 示例 | 六人逐项确认官方边界；三任务族均有输入、成功、失败、owner；禁止把坐标/GT 作为 VLA 输入 | 全员 |
| B | P0 | 完成平台硬件/版本矩阵，准备 G0 headless smoke | `docs/simulation/platform-inventory.md`、命令与预期日志格式 | 主/备机器信息完整；Isaac/Gazebo 候选仅保留可在 D02 验证的版本；1000 步/相机/3 次重启命令可执行 | A/F |
| C | P0 | 冻结最小场景和三任务族资产/数据目录 | `docs/data/task-asset-catalog.md` | 圆柱正常/倒放/倾倒、密集反光、指定格、最多区域、装满搬运均有参数范围和许可；无真实数据项明确标注 | B/F |
| D | P0 | 产出 OpenVLA-OFT 可复现环境清单与统一执行接口差距表 | `docs/models/openvla-oft-environment.md` | 固定官方 commit、Python/CUDA/依赖、权重来源与 hash 方案；逐字段映射 `InferRequest/Response`；列出 D02 smoke | A/B/F |
| E | P0 | 产出 openpi/π0.5 可复现环境清单与动作/norm stats 差距表 | `docs/models/openpi-environment.md` | 固定官方 commit/submodule、JAX/CUDA/依赖、checkpoint/norm stats 身份；逐字段映射统一接口；列出 D02 smoke | A/B/F |
| F | P0 | 建立证据、CI、固定 seed 和官方基线保护 | CI 运行链接、`docs/testing/evidence-convention.md` | 基线哈希、格式、lint、单测全绿；定义原始 JSONL、录像、配置、seed、失败样例索引；失败不得静默忽略 | A/全员 |

## 2. 当日接口冻结点

- 总 Agent 只输出语义 `TaskPlan/Subtask`，不得输出目标坐标、轨迹点或抓取姿态。
- OpenVLA-OFT 与 π0.5 共享观察、动作、安全、取消、超时和错误合同。
- 动作采用 `N × 7`：六维末端增量 + 一个夹爪量；坐标系、单位、频率和
  夹爪符号必须在 D04 前用往返测试冻结。
- 每个执行步骤后必须重新获取观测并核验；失败只重试当前子任务。
- 仿真 GT 只能进入离线 sidecar/评测，不得进入在线 VLA 请求。

## 3. 18:00 集成验收

```powershell
python scripts/verify_official_baselines.py
python scripts/verify_project_frozen_inputs.py
python -m ruff format --check .
python -m ruff check .
python -m pytest -q
git diff --check
```

通过条件：

- [ ] 两份官方 PDF 哈希一致；两张冻结图与 v1.0 DOCX 快照单独校验通过；
- [ ] A-F 都能 clone、创建分支、push 并提交 Draft PR；
- [ ] 每个 PR 都关联 Issue、写清 DoD 和验证命令；
- [ ] B 的 G0 命令能在目标机器启动，或形成当天升级的 P0；
- [ ] D/E 不共享同一环境，不提交权重，不声称 mock 是真实模型；
- [ ] F 给出当前可重复的测试基线。

## 4. 当日最大风险

1. **平台与 GPU 未知**：D02 若无法稳定 headless，不得继续同时维护
   Isaac 和 Gazebo；当晚选择唯一回退。
2. **双 VLA 并行过早**：D/E 今日只做可复现环境和合同映射；任何大规模训练
   必须等 G2 数据 Gate，避免两条高成本链路同时失控。
